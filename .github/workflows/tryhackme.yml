name: Update TryHackMe Badge

on:
  schedule:
    - cron: " * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-badge:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: KeizerSec/Tryhackme-Badge@main
        with:
          username: Mollyman
